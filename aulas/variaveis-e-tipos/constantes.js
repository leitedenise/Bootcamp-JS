//DECLARAÇÃO

/*
Geralmente são declaradas em SNAKE_UPPER_CASE (em letras maiúsculas e separados por underline).
*/

const NOME = 'Denise';
const SEGUNDO_NOME = 'Leite';

//os valores não podem ser alterados. E tbm não é possivel atribuir valores antes de declarar.

/*
VAR- tem escopo global ou local, pode ser redeclarada, pode ser reatribuida, e pode ser atribuido um valor entes da declaração.

LET- tem escopo de bloco, não pode ser redeclarada, pode ser reatribuida e não pode-se atribuir valores entes de declarar.

CONST- tem escopo de bloco, não pode ser redeclarada, não pode ser reatribuida e não pode-se atribuir valor entes da declaração.
*/

