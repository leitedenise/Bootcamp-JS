/*
DOM- Document Object Model

é um padrao de como vc acessa e modifica os elementos de uma pagina web.
São os nós que representam cada elemento da estrutura html da pagina.
*/

/*
BOM- Browser Object Model

ao abrir o browser ele tem a janela window, o bom seria isso, essa arvore window, e dentro dela está o DOM (documento) e outros dados do window.
*/

